export const API_CONFIG = {
    BASE_URL: '',  // Empty string for same-origin requests
    API_URL: '/api'  // Just /api since it's now on the same origin
};


// export const API_CONFIG = {
//     BASE_URL: 'http://localhost:5000',  // Your backend URL
//     API_URL: 'http://localhost:5000/api'  // Full API URL
// };

export default API_CONFIG;
